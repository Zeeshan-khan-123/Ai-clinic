import { COLORS } from "../constants/colors";

export const statusColors = {
  confirmed: COLORS.green,
  pending: COLORS.yellow,
  completed: COLORS.accent,
  cancelled: COLORS.red,
  "no-show": COLORS.textMuted,
  Low: COLORS.green,
  Moderate: COLORS.yellow,
  High: COLORS.red,
  Critical: COLORS.red,
};
